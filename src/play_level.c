#include "cub3D.h"

void        play_level(char *map)
{
	printf("map:\n%s\n", map);
    /*
    while (true)
    {
        if input up/down/left/right key
            change player position
        if 
    }
    */
}